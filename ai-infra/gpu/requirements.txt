# ── GPU requirements — Mistral-7B-v0.3 + ultrachat_200k ──────────────────────
# Versions pinned to avoid the ValueError breaking change in transformers 4.40+
#
# INSTALL ORDER (important):
#   1. pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cu121
#   2. pip install -r requirements_gpu.txt
#   3. pip install flash-attn --no-build-isolation
# -----------------------------------------------------------------------------

# Core — pinned to last versions before transformers 4.40 config lock-down
transformers>=4.40.0          # last version without use_cache lock-down issue
accelerate==0.29.3            # matches transformers 4.39.x
datasets==2.18.0

# Fine-tuning
peft==0.10.0                  # stable, compatible with transformers 4.39.x
bitsandbytes==0.43.1          # 4-bit quantisation (QLoRA)

# Inference serving
vllm==0.4.3                   # pinned — newer versions may require torch 2.4+

# Experiment tracking
wandb==0.17.4

# API server (optional)
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
openai==1.35.7                # for calling vLLM's OpenAI-compatible API

# Utilities
numpy==1.26.4
tqdm==4.66.4
sentencepiece==0.2.0
protobuf==5.27.2
safetensors==0.4.3
huggingface-hub==0.22.2       # matches transformers 4.39.x
packaging==24.1
psutil==5.9.8
nvidia-ml-py==12.555.43       # pynvml — python bindings for nvidia-smi